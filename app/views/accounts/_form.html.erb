<%= form_with(model: account, local: true, class: 'card') do |form| %>
  <% if account.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(account.errors.count, "error") %> prohibited this account from being saved:</h2>
      <ul>
      <% account.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-field">
    <%= form.label :name, 'Name', class: 'form-label' %>
    <%= form.text_field :name, class: 'form-text' %>
  </div>

  <div class="form-actions">
    <%= form.submit 'Update Account', class: 'btn btn-blue mr-6' %>
    <%= link_to 'Discard', account_path(current_user.account_id), class: 'btn-link' %>
  </div>
<% end %>
